# 植物学家 Mod Agent

## Grok 优先委派

- 实现代码、批量重构、大型代码库分析及其他预计高 token 消耗且边界明确的任务，默认优先调用已安装的 `grok-delegate` 技能，由 Grok 执行主要工作，Codex 负责编写任务简报、审查结果、独立验证并提交。
- 简单问答、小范围单文件修改、必须由 Codex 直接判断的任务，或 Grok 不可用/未登录时，可以不委派；回退为 Codex 直接处理时应说明原因。
- 用户明确要求 Codex 直接完成时，以用户当次要求为准。

## Grok 访问

- 调用 Grok 默认直接使用本机网络和本机认证，不设置、不切换、也不启动独立代理。
- 本机 CLI 路径为 `C:\Users\tlaio\.grok\bin\grok.exe`；直接调用前先用 `grok models` 验证登录状态和网络。
- 图生图必须调用 `image_edit`，不要调用 `image_gen`；生成前明确传入 `aspect_ratio`。
- 仅在用户明确要求使用代理时，才使用 `C:\Users\tlaio\.codex\scripts\grok-with-proxy.ps1`。代理包装脚本不得修改、覆盖或删除用户的订阅与代理配置。

本仓库是《杀戮尖塔 2》自定义角色 Mod「植物学家」。实现卡牌、遗物、药水、人物和玩法系统时：

- 先读取并遵循 `sts2mod` 技能，以及仓库内的 `README.md`、`botanist.json`、现有 `Scripts/` 与 `botanist/` 约定。
- 玩法与文案以 [植物学家卡组需求文档.md](植物学家卡组需求文档.md) 为准；其中「种子与培育」是角色核心机制，后续设计与代码不得与下文冲突。
- 先做可加载、可本地化、可在游戏中取得的最小内容，再扩展成熟联动和 UI。

## 文件结构

参考原版 `src/Core/Models` 分类组织项目：内容模型放在 `Scripts/Models/{Cards,CardPools,Characters,Relics,RelicPools,Potions,PotionPools,Powers}`，玩法系统放在 `Scripts/Systems`，Godot 节点放在 `Scripts/Nodes`，补丁放在 `Scripts/Patches`，通用工具放在 `Scripts/Utilities`。尚未实现但已规划的分类使用空目录和 `.gitkeep` 占位。

## 核心机制：种子与培育

### 元素

每张植物学家卡牌携带一种元素标签。首版暂定四种元素：地、火、水、风；也可整体替换为阳光、水分、土壤、空气。元素主要用于培育计数，不应直接等同于能量或伤害类型。

### 种子牌

- 种子不是卡牌类型，而是拥有「成长」关键词和种子标签的卡牌；每张种子仍必须声明真实的攻击、技能、能力、状态或诅咒类型。
- 种子牌打出时可以根据平衡选择是否执行一个较弱的“播种效果”；没有播种效果的种子，应通过更低需求、更顺畅的成熟路线或更强成熟收益补偿打出节奏。原卡实体进入培养区承载培育，不立即进入弃牌堆；重放只增加培育记录，不复制卡牌实体。
- 每张种子牌拥有 1—3 种所需元素及对应计数（建议总需求 2—8 点）。
- 种子成熟后执行较强的成熟效果；原卡首次成长时按自身卡牌类型离场一次，重放产生的其余培育实例继续独立结算并直接消失，不会生成额外卡牌。
- 当培养区已满时，打出的新种子不进入培养区；若该牌有播种效果则照常结算，具体例外由单张卡牌文字说明。
- 种子在培养区中的状态应能让玩家理解名称、元素需求、当前进度和成熟效果。

### 培养区

- 角色专属空间，初始 3 个位置。
- 位置满时，不能容纳新种子；新种子按“打出—结算播种效果（若有）—按原卡牌类型离场”处理。
- 每回合开始、结束或特定卡牌结算时获得元素并减少对应种子的计数。具体触发点应在实现前统一，首版建议“打出带元素卡牌时减少 1 点对应元素”。
- 同一张卡只减少一次计数；万能元素可同时作用于所有种子。
- 所需计数全部为 0 后立即成熟，成熟效果按培养区顺序或卡牌注明的顺序结算。
- 卡牌效果不得让玩家自由选择培养区中的某一颗种子；只能按前 N 颗、后 N 颗、全体或其他固定顺序范围结算。种子的种植顺序是策略的一部分。
- 战斗结束时，培养区内所有未成熟种子移除，不进入下一场战斗；若有例外，须由遗物或卡牌文字明确说明。

### 万能元素

初始遗物「异界烙印」在每场战斗开始时提供 1 张万能元素牌。万能元素是衍生消耗牌，打出后使培养区中所有种子的每种对应元素计数各减少 1（最低为 0）。

### 火堆嫁接

- 植物学家的火堆提供「嫁接」选项，进入后以砧木、结果预览、接穗三张牌显示；确认才从牌组移除接穗并消耗本次火堆行动。
- 嫁接只转移接穗的成长效果与成长需求，不转移打出、播种、关键词或费用效果。需求按每种元素执行 `砧木当前有效需求 + ceil(接穗需求 / 2)`，接穗需求只取整一次。
- 升级状态在嫁接时快照；带附魔或已有嫁接的卡不能作为接穗，植物组织不能作为接穗但可以作为砧木。同一实际卡牌不能同时作为砧木和接穗。
- 嫁接记录随砧木保存、复制和联机同步；成熟时先执行砧木成长，再按嫁接顺序执行接穗成长，且整颗种子只获得 1 点「成长」关键词能量。

## 实现约束

首版把培育触发统一为：打出带元素标签的卡牌时，培养区中需要该元素的种子各减少 1 点对应计数。不要同时再做回合开始/结束自动扣点，除非后续明确改规则。

- 元素是培育标签，不是能量、费用或伤害类型；不要用原版能量色或伤害属性冒充元素。
- 种子必须用种子标签和「成长」关键词表达，不得新增卡牌类型；卡面和卡牌类型仍按其真实攻击、技能、能力、状态或诅咒显示。打出流程固定为：结算播种效果（若有）-> 这张牌的元素只作用于培养区里已有的种子 -> 有空位则新增一条培育实例，满则不新增并让原卡按自身卡牌类型离场。培育期间原卡实体留在战斗内；第一颗对应培育实例成长时，原卡按自身卡牌类型离场一次，其余培育实例继续独立结算。重放不会复制卡牌实体，成熟效果始终由原卡结算。种子不会用自己的元素给自己扣点。
- 培养区是角色战斗内空间，初始容量 3。状态对玩家可见：名称、所需元素、剩余计数、成熟效果。
- 一张牌只触发一次扣点。万能元素对培养区所有种子生效：每种所需元素各 -1，最低 0。
- 计数全部为 0 时立即成熟：先按原卡结算成熟效果；原卡首次成长时按自身卡牌类型离场，随后移除对应培育实例。其余培育实例继续留在培养区独立结算。多枚同时成熟时按培养区顺序或卡牌注明顺序结算。
- 战斗结束清空未成熟种子；跨战斗保留必须由遗物或卡牌文字显式声明。
- 初始卡组必须包含 1 张种子；万能元素由初始遗物「异界烙印」在每场战斗开始时提供，不进入初始卡组。
- 单张种子不得同时做到高额即时伤害、高额防御和低成本成熟。没有成熟种子时，工具/技巧牌仍须能完成防御与过渡。
- 能力牌最低稀有度为罕见；普通和基础牌不得为能力。
- 衍生卡成本等于自身成本加生成它的原卡成本；原卡价值额外加入一张衍生卡的价值与成本，生成多张时价值项只计一张；原卡成本仍为每张衍生牌追加 0.6 点牌位成本。生成链逐层累加。
- 项目进度只统计可收集卡牌；标记为 `CardRarity.Token` 的衍生卡只列明细和衍生牌计数，不计入卡牌完成度、类型、稀有度、元素或种子覆盖率。
- 卡面文案直接写效果：打出效果不加「打出时：」前缀；成熟效果统一以「成长：」开头，不写「成熟时：」。
- 每张卡牌类所在的源码文件开头必须用简体中文注释标明「中文卡名」和「卡面描述」；描述以 `botanist/localization/zhs/cards.json` 为准，多行卡面按行注释，文案变更时同步更新注释。

改卡牌、遗物、药水、UI 或战斗补丁前，先核对本文件与需求文档是否被改动行为打破。

## 美术工具链

- 角色立绘、透明部件和特效贴图优先使用已安装的 `imagegen`、`transparent-visual-assets`、`sprite-animation-assets` 技能。
- 角色动作和 Spine VFX 使用 `spine-animation`、`2d-character-rig-animation` 技能，并以游戏当前 Spine 4.2 资源格式为准。
- `spine-animation` 技能采用 PolyForm Noncommercial 1.0.0；商业用途不得直接使用，需另行取得授权。
- 技能脚本统一使用 Codex 内置 Python：`C:\Users\tlaio\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`。
- **卡图第一优先级是符合《杀戮尖塔 2》原版卡图画风。** 植物辨识度、机制意象、构图变化和画面完成度都排在其后；画风不对的结果一律作废，不得因为植物准确、构图好看或用户未立刻反对而当作有效候选。
- 原版卡图画风指：二维游戏卡牌插画，大块不透明色面、硬边、粗重深色轮廓、强烈剪影、有限配色、浅层笔触和图形化光影。不合格包括写实、油画厚涂融合、照片、3D、动漫赛璐珞、光滑渐变主导，以及没有粗轮廓和大色块的插画。
- 生成卡图时默认使用 Grok 图生图并调用 `image_edit`，因为这是对齐原版画风的默认方法；仅在用户明确指定时改用其他图像生成工具。改用其他工具后仍必须先过画风关，达不到原版卡图画风就停下来改方法，不把错误画风的成品交给用户当卡图候选。
- 卡图的视觉参考只使用《杀戮尖塔 2》原版游戏卡图，不引入站外图片、摄影、影视、其他游戏或第三方插画等额外参考内容。
- 参考输入默认不使用本项目已有卡图、角色图、特效图或此前生成的候选图；图生图同样只以《杀戮尖塔 2》原版内容作参考。确有需要沿用项目资产时，必须先得到用户明确指定。
- 生成卡图前先检索《杀戮尖塔 2》原版中主题、效果或气质相近的多张卡图，**先锁定其平面图形语言**，再核对卡名对应的现实原型、卡面效果、元素归属与名称意象。综合参考原版画风、构图习惯、光影、笔触、材质和背景处理，但不得照搬原卡主体或构图。在已经符合原版画风的前提下，现实植物优先保留可辨识特征，再通过构图和光效表达卡牌机制，不得只按牌名做字面联想。
- 最终卡图不含卡框、文字、UI、水印或数字。
- 每张卡默认一次生成至少 4 张候选图供用户选择：必须先统一到原版卡图画风，再拉开主题表现、构图和光效差异。未确认选中方案前，不覆盖正式卡图资源或 `PortraitPath`。

## 部署

- 部署必须运行 `tools/DeployBotanist.ps1`，由它同时生成并校验 DLL、manifest、PCK 后再复制到游戏目录。
- 禁止用 `dotnet build` 后手工复制 DLL/manifest；普通构建不得再向游戏目录做半更新。
- 脚本只使用本机 Godot 4.5 Mono；找不到时应明确报错并提供 `-GodotPath`，不得自动下载 Godot。
